# Archived Documentation

This directory is intentionally minimized to keep active context small.

Historical planning, prompt packs, and one-off reports were pruned during
reduction passes on 2026-03-05/06. Those artifacts remain available in git
history if needed for forensic review.

## Archive Structure

- `frontend/README.md` - summary of removed frontend planning artifacts
- `reports/README.md` - summary of removed one-off report artifacts

## Canonical Active Docs

Use active docs in `docs/` and the root `README.md` for current behavior.
